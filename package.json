{
  "name": "crdt-collaborative",
  "version": "1.0.0",
  "description": "CRDT-based collaborative workspace — proof of concept",
  "scripts": {
    "build": "vite build",
    "build:watch": "vite build --watch",
    "start:server": "ts-node src/server.ts",
    "test": "jest --forceExit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.17.0",
    "@types/ws": "^8.5.13",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0",
    "vite": "^8.0.14"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.ts"
    ]
  }
}
